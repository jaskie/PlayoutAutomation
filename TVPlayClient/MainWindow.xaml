<Window x:Class="TVPlayClient.MainWindow"
        ResxExtension.DefaultResxName="TVPlayClient.MainWindow" Language="{UICulture}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TAS.Client"
        xmlns:views="clr-namespace:TAS.Client.Views;assembly=TAS.Client"
        xmlns:vm="clr-namespace:TAS.Client.ViewModels;assembly=TAS.Client"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="{Resx _windowTitle}" 
        Height="600" 
        Width="1024"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <local:MainWindowViewmodel x:Key="vm"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource vm}">
        <xctk:BusyIndicator IsBusy="{Binding IsLoading}">
            <xctk:BusyIndicator.BusyContent>
                <TextBlock Text="{Resx ResxName=TVPlayClient.MainWindow, Key=_pleaseWait}"/>
            </xctk:BusyIndicator.BusyContent>
            <ContentControl Content="{Binding View}"/>
        </xctk:BusyIndicator>
    </Grid>
</Window>
